{
  "name": "trip-planner",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["client", "server"],
  "scripts": {
    "start": "concurrently -n server,client -c blue,green \"npm run start --workspace server\" \"npm run dev --workspace client\"",
    "dev": "npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
